{
  "name": "conexao-mysql-node",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "mysqld --initialize-insecure --user=mysql && mysqld --user=mysql & sleep 5 && mysql -e 'CREATE DATABASE IF NOT EXISTS railway;' && node index.js"
  },
  "dependencies": {
    "mysql2": "^3.11.0"
  }
}
